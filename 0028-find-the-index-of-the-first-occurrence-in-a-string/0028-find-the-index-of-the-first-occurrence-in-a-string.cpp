class Solution {
public:
    int strStr(string haystack, string needle) {
        // if(needle.length()>haystck.length())return -1;
        // unordered_map<vector<string>,int>mpp;
        // int size=needle.length();
        // for(int i=0;i<=haystack.length()-size;i++)
        // {
        //     string str=
        // }
        int x=haystack.find(needle);
        return x;
    }
};